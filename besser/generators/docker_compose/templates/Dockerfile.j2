# Generated by BESSER DockerComposeGenerator (6b-2) — BAF agent build context.
FROM python:3.11-slim

WORKDIR /app

# BESSER Agentic Framework + all optional extras (LLM/RAG/DB connectors).
RUN pip install --no-cache-dir "besser-agentic-framework[all]"

# The generated agent script + its config land in this build context.
COPY . .

# Configure LLM credentials etc. in config.yaml before running.
CMD ["python", "{{ agent_script }}"]
