{% set ocl_ns = namespace(needs_re=false, needs_model_validator=false) -%}
{% if constraints_map -%}
    {% for constraint_list in constraints_map.values() -%}
        {% for constraint in constraint_list -%}
            {% if constraint.uses_re -%}
                {% set ocl_ns.needs_re = true -%}
            {% endif -%}
            {% if constraint.model_expression is defined -%}
                {% set ocl_ns.needs_model_validator = true -%}
            {% endif -%}
        {% endfor -%}
    {% endfor -%}
{% endif -%}
{% if ocl_ns.needs_re %}
import re
{% endif %}
from datetime import datetime, date, time
from typing import Any, List, Optional, Union, Set
from enum import Enum
from pydantic import BaseModel, field_validator{{ ", model_validator" if ocl_ns.needs_model_validator else "" }}

{% set ns = namespace(abstract_found=false, id_found = false, same_name_found = false, has_field = false )-%}
{% for class in domain.get_classes() -%}
    {% if class.is_abstract -%}
        {% set ns.abstract_found = true -%}
    {% endif -%}    
{% endfor -%}
{% if ns.abstract_found %}
from abc import ABC, abstractmethod
{% endif %}

############################################
# Enumerations are defined here
############################################

{# Handle enums first #}
{% for enum in domain.get_enumerations() -%}
class {{ enum.name }}(Enum):
{% for literal in enum.literals %}
    {{ literal.name }} = "{{ literal.name }}"
{% endfor %}

{% endfor -%}

{% if backend and assoc_link_meta %}
############################################
# Association-class link payloads
############################################

{% for link in assoc_link_meta %}
class {{ link.link_class }}(BaseModel):
    target: int  # primary key of the linked entity
{% for attr in link.attributes %}
{% set link_attr_type = 'Any' if attr.type_name == 'any' else attr.type_name %}
    {{ attr.name }}: {{ ('Optional[' ~ link_attr_type ~ '] = None') if attr.optional else link_attr_type }}
{% endfor %}

{% endfor %}
{% endif %}
############################################
# Classes are defined here
############################################
{% for class in sorted_classes %}
class {{ class.name }}{% if backend %}Create{% endif %}{% if class.parents() %}({% for parent in class.parents() %}{{parent.name}}{% if backend %}Create{% endif %}{{ ", " if not loop.last else "" }}{% endfor %}){% elif class.is_abstract %}(ABC, BaseModel){% else %}(BaseModel){% endif %}:
    {% set ns.id_found = false -%}
    {% set ns.has_field = false %}
    {%- set inherited_attr_names = [] -%}
    {%- if class.parents() -%}
        {%- for attr in class.inherited_attributes() -%}
            {%- do inherited_attr_names.append(attr.name) -%}
        {%- endfor -%}
    {%- endif %}
    {% for attribute in class.attributes %}
        {%- if attribute.is_id or attribute.name == 'id' %}
            {%- set ns.id_found = true %}
        {%- endif %}
        {#- Create schemas (backend mode) never carry server-owned fields:
            - the surrogate `id` (the DB autoincrements it, so a client-sent
              id is at best ignored and at worst a PK conflict), and
            - audit timestamps `createdAt`/`updatedAt` (the ORM stamps them via
              server-side defaults / onupdate), which a client must not send --
              leaving them in Create forces the client to supply values the
              server owns and breaks the generated app.
            A declared primary key -- whether named `id` or anything else
            (e.g. isbn, code) -- is client-supplied and stays in the schema. #}
        {%- set _norm_name = attribute.name | lower | replace('_', '') %}
        {%- set _is_server_owned = backend and not attribute.is_id and (attribute.name == 'id' or _norm_name in ['createdat', 'updatedat']) %}
        {%- if attribute.name not in inherited_attr_names and not _is_server_owned %}
        {%- set is_opt = attribute.is_optional and not attribute.is_id %}
        {%- set attr_type_name = 'Any' if attribute.type.name == 'any' else attribute.type.name %}
        {%- if is_opt and attribute.default_value is not none %}
    {{ attribute.name }}: Optional[{{ attr_type_name }}] = {% if attribute.type.name == 'str' %}"{{ attribute.default_value }}"{% elif attribute.type.__class__.__name__ == 'Enumeration' %}{% set _dv = attribute.default_value | string %}{% if _dv.startswith(attr_type_name ~ '.') %}{{ _dv }}{% else %}{{ attr_type_name }}.{{ _dv }}{% endif %}{% elif attribute.type.name == 'bool' %}{{ 'True' if (attribute.default_value | string | lower) in ['true', '1', 'yes', 'on'] else 'False' }}{% else %}{{ attribute.default_value }}{% endif %}
        {%- elif is_opt %}
    {{ attribute.name }}: Optional[{{ attr_type_name }}] = None
        {%- elif attribute.default_value is not none %}
    {{ attribute.name }}: {{ attr_type_name }} = {% if attribute.type.name == 'str' %}"{{ attribute.default_value }}"{% elif attribute.type.__class__.__name__ == 'Enumeration' %}{% set _dv = attribute.default_value | string %}{% if _dv.startswith(attr_type_name ~ '.') %}{{ _dv }}{% else %}{{ attr_type_name }}.{{ _dv }}{% endif %}{% elif attribute.type.name == 'bool' %}{{ 'True' if (attribute.default_value | string | lower) in ['true', '1', 'yes', 'on'] else 'False' }}{% else %}{{ attribute.default_value }}{% endif %}
        {%- else %}
    {{ attribute.name }}: {{ attr_type_name }}
        {%- endif %}{% if attribute.is_id %}  # the id{% endif %}
    {% set ns.has_field = true %}

        {% endif %}
    {% endfor %}
    {%- if not ns.id_found and not backend and not class.parents() %}
    id: int  # id created
    {% set ns.has_field = true %}
    {% endif %}
    {%- if backend and assoc_end_fields and class.name in assoc_end_fields %}
    {%- for ef in assoc_end_fields[class.name] %}
    {{ ef.name }}: int  # association end -> {{ ef.type_name }} primary key
    {% set ns.has_field = true %}
    {%- endfor %}
    {%- endif %}
    {%- for association in class.associations %}
        {%- if association.ends|length == 2 %}
            {%- set link_class = assoc_by_association.get(association.name) if backend and assoc_by_association else none %}
            {%- if link_class %}
            {# The association carries an association class: links travel as
               <AssocClass>LinkCreate payloads (target id + association attributes). #}
            {%- for end in association.ends %}
                {%- if end.type.name != class.name %}
                    {%- if end.multiplicity.max > 1 %}
                        {%- if end.multiplicity.min == 0 %}
    {{ end.name }}: Optional[List[Union["{{ link_class }}", int]]] = None  # via association class (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ end.name }}: List[Union["{{ link_class }}", int]]  # via association class
    {% set ns.has_field = true %}
                        {%- endif %}
                    {%- else %}
    {{ end.name }}: Optional[Union["{{ link_class }}", int]] = None  # via association class (link is created from the other side or via its own endpoint)
    {% set ns.has_field = true %}
                    {%- endif %}
                {%- endif %}
            {%- endfor %}
            {%- else %}
            {%- set ends_list = association.ends | list %}
            {%- set is_self_assoc = (ends_list[0].type.name == ends_list[1].type.name) %}
            {%- if is_self_assoc %}
                {%- for i in range(2) %}
                    {%- set ns.end1 = ends_list[(i + 1) % 2] %}
                    {%- set ns.end2 = ends_list[i] %}
                {%- if ns.end1.multiplicity.max > 1 and ns.end2.multiplicity.max > 1 %}
                    {%- if nested_creations %}
                        {%- if backend %}
    {{ ns.end2.name }}: Optional[List[Union["{{ ns.end2.type.name }}Create", {{ pk_types.get(ns.end2.type.name, 'int') }}]]] = None  # N:M Relationship
    {% set ns.has_field = true %}
                        {% endif %}
                        {%- if not backend %}
                            {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional[Set["{{ ns.end2.type.name }}"]] = None  # N:M Relationship (optional)
    {% set ns.has_field = true %}
                            {%- else %}
    {{ ns.end2.name }}: Set["{{ ns.end2.type.name }}"]  # N:M Relationship
    {% set ns.has_field = true %}
                            {%- endif %}
                        {%- endif %}
                    {%- else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional[List[{{ pk_types.get(ns.end2.type.name, 'int') }}]] = None  # N:M Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: List[{{ pk_types.get(ns.end2.type.name, 'int') }}]  # N:M Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- elif ns.end1.multiplicity.max > 1 and ns.end2.multiplicity.max == 1 %}
                    {%- if backend %}
                        {%- set has_fk = (fkeys.get(association.name, [''])[0] == class.name) -%}
                        {%- if has_fk %}
                            {%- if ns.end2.multiplicity.min > 0 %}
    {{ ns.end2.name }}: {{ pk_types.get(ns.end2.type.name, 'int') }}  # N:1 Relationship (mandatory)
    {% set ns.has_field = true %}
                            {% else %}
    {{ ns.end2.name }}: Optional[{{ pk_types.get(ns.end2.type.name, 'int') }}] = None  # N:1 Relationship (optional)
    {% set ns.has_field = true %}
                            {% endif %}
                        {%- endif %}
                    {% else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional["{{ ns.end2.type.name }}"] = None  # N:1 Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: "{{ ns.end2.type.name }}"  # N:1 Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- elif ns.end1.multiplicity.max == 1 and ns.end2.multiplicity.max > 1 %}
                    {%- if backend %}
    {{ ns.end2.name }}: Optional[List[{{ pk_types.get(ns.end2.type.name, 'int') }}]] = None  # 1:N Relationship
    {% set ns.has_field = true %}
                    {% else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional[List["{{ ns.end2.type.name }}"]] = None  # 1:N Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: List["{{ ns.end2.type.name }}"]  # 1:N Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- elif ns.end1.multiplicity.max == 1 and ns.end2.multiplicity.max == 1 %}
                    {%- if backend %}
                        {%- set has_fk = (fkeys.get(association.name, [''])[0] == class.name) -%}
                        {%- if has_fk %}
                            {%- if ns.end2.multiplicity.min > 0 %}
    {{ ns.end2.name }}: {{ pk_types.get(ns.end2.type.name, 'int') }}  # 1:1 Relationship (mandatory)
    {% set ns.has_field = true %}
                            {% else %}
    {{ ns.end2.name }}: Optional[{{ pk_types.get(ns.end2.type.name, 'int') }}] = None  # 1:1 Relationship (optional)
    {% set ns.has_field = true %}
                            {% endif %}
                        {%- else %}
                            {%- if ns.end2.multiplicity.min > 0 %}
    {{ ns.end2.name }}: {{ pk_types.get(ns.end2.type.name, 'int') }}  # 1:1 Relationship (mandatory)
    {% set ns.has_field = true %}
                            {% else %}
    {{ ns.end2.name }}: Optional[{{ pk_types.get(ns.end2.type.name, 'int') }}] = None  # 1:1 Relationship (optional)
    {% set ns.has_field = true %}
                            {% endif %}
                        {%- endif %}
                    {% else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional["{{ ns.end2.type.name }}"] = None  # 1:1 Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: "{{ ns.end2.type.name }}"  # 1:1 Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- endif %}
                {%- endfor %}
            {%- else %}
            {%- set ns.end1 = none %}
            {%- set ns.end2 = none %}
            {%- for end in association.ends %}
                {%- if end.type.name == class.name %}
                    {%- set ns.end1 = end %}
                {%- else %}
                    {%- set ns.end2 = end %}
                {%- endif %}
            {%- endfor %}
            {%- if ns.end1 and ns.end2 and ns.end2.is_navigable %}
                {%- if ns.end1.multiplicity.max > 1 and ns.end2.multiplicity.max > 1 %}
                    {%- if nested_creations %}
                        {%- if backend %}
    {{ ns.end2.name }}: Optional[List[Union["{{ ns.end2.type.name }}Create", {{ pk_types.get(ns.end2.type.name, 'int') }}]]] = None  # N:M Relationship
    {% set ns.has_field = true %}
                        {% endif %}
                        {%- if not backend %}
                            {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional[Set["{{ ns.end2.type.name }}"]] = None  # N:M Relationship (optional)
    {% set ns.has_field = true %}
                            {%- else %}
    {{ ns.end2.name }}: Set["{{ ns.end2.type.name }}"]  # N:M Relationship
    {% set ns.has_field = true %}
                            {%- endif %}
                        {%- endif %}
                    {%- else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional[List[{{ pk_types.get(ns.end2.type.name, 'int') }}]] = None  # N:M Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: List[{{ pk_types.get(ns.end2.type.name, 'int') }}]  # N:M Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- elif ns.end1.multiplicity.max > 1 and ns.end2.multiplicity.max == 1 %}
                    {%- if backend %}
                        {%- set has_fk = (fkeys.get(association.name, [''])[0] == class.name) -%}
                        {%- if has_fk %}
                            {%- if ns.end2.multiplicity.min > 0 %}
    {{ ns.end2.name }}: {{ pk_types.get(ns.end2.type.name, 'int') }}  # N:1 Relationship (mandatory)
    {% set ns.has_field = true %}
                            {% else %}
    {{ ns.end2.name }}: Optional[{{ pk_types.get(ns.end2.type.name, 'int') }}] = None  # N:1 Relationship (optional)
    {% set ns.has_field = true %}
                            {% endif %}
                        {%- endif %}
                    {% else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional["{{ ns.end2.type.name }}"] = None  # N:1 Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: "{{ ns.end2.type.name }}"  # N:1 Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- elif ns.end1.multiplicity.max == 1 and ns.end2.multiplicity.max > 1 %}
                    {%- if backend %}
    {{ ns.end2.name }}: Optional[List[{{ pk_types.get(ns.end2.type.name, 'int') }}]] = None  # 1:N Relationship
    {% set ns.has_field = true %}
                    {% else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional[List["{{ ns.end2.type.name }}"]] = None  # 1:N Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: List["{{ ns.end2.type.name }}"]  # 1:N Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- elif ns.end1.multiplicity.max == 1 and ns.end2.multiplicity.max == 1 %}
                    {%- if backend %}
                        {%- set has_fk = (fkeys.get(association.name, [''])[0] == class.name) -%}
                        {%- if has_fk %}
                            {%- if ns.end2.multiplicity.min > 0 %}
    {{ ns.end2.name }}: {{ pk_types.get(ns.end2.type.name, 'int') }}  # 1:1 Relationship (mandatory)
    {% set ns.has_field = true %}
                            {% else %}
    {{ ns.end2.name }}: Optional[{{ pk_types.get(ns.end2.type.name, 'int') }}] = None  # 1:1 Relationship (optional)
    {% set ns.has_field = true %}
                            {% endif %}
                        {%- else %}
                            {%- if ns.end2.multiplicity.min > 0 %}
    {{ ns.end2.name }}: {{ pk_types.get(ns.end2.type.name, 'int') }}  # 1:1 Relationship (mandatory)
    {% set ns.has_field = true %}
                            {% else %}
    {{ ns.end2.name }}: Optional[{{ pk_types.get(ns.end2.type.name, 'int') }}] = None  # 1:1 Relationship (optional)
    {% set ns.has_field = true %}
                            {% endif %}
                        {%- endif %}
                    {% else %}
                        {%- if ns.end2.multiplicity.min == 0 %}
    {{ ns.end2.name }}: Optional["{{ ns.end2.type.name }}"] = None  # 1:1 Relationship (optional)
    {% set ns.has_field = true %}
                        {%- else %}
    {{ ns.end2.name }}: "{{ ns.end2.type.name }}"  # 1:1 Relationship
    {% set ns.has_field = true %}
                        {%- endif %}
                    {% endif %}
                {%- endif %}
            {%- endif %}
            {%- endif %}
            {%- endif %}
        {%- endif %}
    {%- endfor %}
    {%- if not ns.has_field %}
    pass
    {% endif %}
    {# OCL Constraint Validators #}
    {%- if constraints_map and class.name in constraints_map %}
    {%- for constraint in constraints_map[class.name] %}
    {%- if constraint.skipped %}

    # NOTE: OCL constraint '{{ constraint.constraint_name }}' involves collections/relationships and is not enforced by this Create model.
    {%- elif constraint.model_expression is defined %}

    @model_validator(mode='after')
    def validate_{{ constraint.validator_name }}(self):
        """OCL Constraint: {{ constraint.constraint_name }}"""
        if not ({{ constraint.model_expression }}):
            raise ValueError({{ constraint.message_repr }})
        return self
    {%- else %}

    @field_validator('{{ constraint.property }}')
    @classmethod
    def validate_{{ constraint.property }}_{{ loop.index }}(cls, v):
        """OCL Constraint: {{ constraint.constraint_name }}"""
        {% if constraint.python_expression is defined %}
        if not ({{ constraint.python_expression }}):
            raise ValueError({{ constraint.message_repr }})
        {% else %}
        if not (v {{ constraint.python_operator }} {{ constraint.value_repr }}):
            raise ValueError({{ constraint.message_repr }})
        {% endif %}
        return v
    {%- endif %}
    {%- endfor %}
    {%- endif %}

{% set ascii_name = class.name | ascii_identifier %}
{% if ascii_name and ascii_name != class.name and ascii_name not in class_names %}
{{ ascii_name }}{% if backend %}Create{% endif %} = {{ class.name }}{% if backend %}Create{% endif %}
{% endif %}

{# Spacing #}
{% endfor %}
